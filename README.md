# ism2411-data-cleaning-copilot

This is a python project that cleans a messy data set by using pandas and GitHub Copilot.
Place `sales_data_raw.csv` in `data/raw/` and run `python3 src/data_cleaning.py` to create `data/processed/sales_data_clean.csv`.
